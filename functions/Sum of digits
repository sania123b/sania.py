def sum_of_digits(n):
    """
    Function to compute the sum of digits in a number.
    
    Parameters:
    n (int): The number to compute the sum of its digits.
    
    Returns:
    int: The sum of digits in the number.
    """
    sum = 0
    while n > 0:
        digit = n % 10
        sum += digit
        n //= 10
    return sum

# Example usage:
number = 12345
print("The sum of digits in", number, "is", sum_of_digits(number))
